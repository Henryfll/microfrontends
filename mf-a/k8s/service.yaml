apiVersion: v1
kind: Service
metadata:
  name: mf-a
  labels:
    app: mf-a
    tier: microfrontend
    framework: angular
    scenario: module-federation
spec:
  type: NodePort
  selector:
    app: mf-a
  ports:
    - name: http
      protocol: TCP
      port: 4201
      targetPort: 4201
      nodePort: 30201
