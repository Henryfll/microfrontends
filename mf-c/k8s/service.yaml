apiVersion: v1
kind: Service
metadata:
  name: mf-c
  labels:
    app: mf-c
    tier: microfrontend
    framework: vue
    scenario: module-federation
spec:
  type: NodePort
  selector:
    app: mf-c
  ports:
    - name: http
      protocol: TCP
      port: 4203
      targetPort: 4203
      nodePort: 30203
