apiVersion: v1
kind: Service
metadata:
  name: host
  namespace: single-spa-experiment
  labels:
    app: host
    orchestrator: single-spa
    experiment: microfrontend-study
spec:
  type: NodePort
  selector:
    app: host
    orchestrator: single-spa
    experiment: microfrontend-study
  ports:
    - name: http
      protocol: TCP
      port: 4200
      targetPort: 4200
      nodePort: 30200
