apiVersion: v1
kind: Service
metadata:
  name: mf-b
  namespace: single-spa-experiment
  labels:
    app: mf-b
    orchestrator: single-spa
    experiment: microfrontend-study
spec:
  type: NodePort
  selector:
    app: mf-b
    orchestrator: single-spa
    experiment: microfrontend-study
  ports:
    - name: http
      protocol: TCP
      port: 4202
      targetPort: 4202
      nodePort: 30202
